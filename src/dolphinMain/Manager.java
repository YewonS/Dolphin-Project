package dolphinMain;

/**
 * Interface Manager is to make sure that all classes implements Manager,
 * which means all those who are managers of Dolphin Swimming Club,
 * should have the specific function.
 */

public interface Manager {

    void selectedOption(int option);

}
